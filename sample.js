#!/usr/local/bin/node
// THIS IS A SAMPLE file

var mysql=require('mysql')

var foo='bar'

var baz='bif'

console.warn(foo)

console.log(jackylpuss)

var conn=new mysql.connection("something","something","something")

var base_url = base_toon_url(toons[i]['realm'], toons[i]['toon_name'], toons[i]['country']);
var fbml += '<li><b><a href="'.url.'">'.toons[i]['toon_name'].'</a></b> ('.toons[i]['realm'].')';